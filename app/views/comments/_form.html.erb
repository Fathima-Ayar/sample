  <%= form_for([current_user, @article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
    <%= hidden_field_tag 'comment[commenter]', current_user.username %>
  </p>
  <p>
    <%= f.submit %>
  </p>
  <% end %>

